# 전사 자산관리 시스템 — 기획안

> 재개발용 스펙. 현재 운영 중인 단일 HTML 대시보드(`SJ_대시보드_V16.html`, 라이브: https://gerrardbak08.github.io/asset-dashboard/) 의 구조·데이터·기능을 그대로 옮겨 적은 문서.
> 작성: 2026-05-09

---

## 1. 목적과 사용자

### 1.1 목적
- 전사 보유 자산(유형/무형/비품)·건물(15동)·사업장(2,000+)·사업장별 비품 운영 현황을 **한 화면에 통합**.
- **MoM(전월 대비)** 변동 자동 표시 — 의사결정·이상치 감지·월마감 회의용.
- 단일 HTML 파일로 배포 가능(이메일/zip/Pages) — **인프라 의존 0**.

### 1.2 타깃 사용자(역할 4종)
| 역할 | 보이는 탭 | 우선 정보 |
|---|---|---|
| **경영진(exec)** | 자산 현황·건물 상세 | 총자산 KPI, MoM, 주의사항, 임대 위험 건물, 건물용도별 현황 |
| **자산관리팀(ops)** | 자산 현황·건물 상세·데이터 조정 | 비품 흐름, 카테고리 변동, 데이터 업로드, 원장 |
| **사업장 관리자(store)** | 사업장 검색 전용 뷰 | 매장별 자산 장부가, 비품 카테고리 TOP 10, 활동 타임라인 |
| **관리자(admin)** | 모든 탭 + 관리자 패널 | CSV/xlsx/xlsb 업로드, 양식 다운로드, PDF 보고서 |

### 1.3 핵심 KPI 시점 기준
- 당월: **2026.03 마감**
- 전월: **2026.02 마감**
- MoM 자동 계산: `(당월 - 전월) / 전월 × 100`

---

## 2. 정보 구조 (IA)

### 2.1 상단 공통 영역
1. **Topbar (sticky)**
   - 좌: 로고(다이소 CI 모티프) + "전사 자산관리 시스템" + 기준월
   - 우(데스크톱): 역할 토글 4개 · 보고서(PDF) · 공유
   - 모바일(≤900px): 역할 토글이 2번째 sticky 행으로 분리
2. **Sticky Nav (메인 탭)**
   - 자산 현황 / 건물 상세 / 데이터 조정 (역할별 가시성)

### 2.2 페이지 트리

```
└─ 자산 현황 (asset)
    ├─ 개요 — Hero KPI, 도넛, MoM, 주의사항, 임원용 요약
    ├─ 자산 상세현황 — 매트릭스(유형/무형/비품 × 본사/매장/물류)
    ├─ 자산 드릴다운 — 자산유형별 원장 검색 + 표
    ├─ 비품 운영 — 카테고리별 재고/구매/이동/폐기 KPI
    ├─ 비품 흐름 — 품목별 Top 10 차트 (구매·이동·폐기·잔존비)
    ├─ 취득 연혁 — 연도별 취득 추이·누적
    └─ 임대 현황 — 지역별 임대율, 위험도 분류
└─ 건물 상세 (building)
    ├─ 카드 그리드 (15건물) — 사진·취득가·임대율·임차인
    ├─ 필터 (지역/용도) — 단순화 4개 그룹: 창고시설·판매시설·제1종근린생활시설·공동주택·업무시설
    ├─ 정렬 — 기본=취득일 최신 / 취득가 / 취득일자
    ├─ 상세 드로어 — 기본정보·임대현황·관리이력·메모·지도
    └─ 건물정보 조정 (admin) — 인라인 편집 표
└─ 데이터 조정 (dataEdit)
    ├─ 양식 다운로드 (CSV)
    └─ 업로드 — 자산/원장_자산/원장_비품/비품운영/건물현황
└─ 관리자 패널 (admin)
    ├─ 로그인 모달 (ID/PW)
    └─ 5종 데이터 카드 + 업로드 버튼
└─ 사업장 관리자 뷰 (store role only)
    ├─ 검색 (조직명/사업장)
    ├─ KPI 4종 (자산 장부가·항목 수·비품 재고액·비품 종수)
    ├─ 비품 카테고리 TOP 10 차트 (금액/수량 토글)
    └─ 자산 유형별 구성 도넛
└─ PDF 보고서 (modal) — 역할별 맞춤 인쇄 미리보기
```

---

## 3. 데이터 모델

### 3.1 외부 소스
| 파일 | 용도 |
|---|---|
| `자산관리_대시보드_건물현황 20260406.xlsx` | 15건물 마스터 (이름·주소·용도·연면적·취득일자·임대율 등) |
| `대시보드 데이터_26년3월마감.xlsb` | 당월 자산현황 + 비품(프로세스별) + 원장_자산/비품/구매/이동/폐기 |
| `대시보드 데이터_26년2월마감.xlsb` | 전월 — MoM 비교용 |
| `건물사진/*.png` (15장) | 건물 정면 사진 |
| `건물상세/*.png` (4장) | 강릉·세종·속초·창원 상세 팝업 사진 |

### 3.2 내부 도메인 객체

```ts
type Building = {
  id: string;                // bd_001 ...
  name: string;              // "속초본점 건물"
  address: string;
  use: string;               // raw 용도 (다중 분류 가능)
  area: { sqm: number; pyeong: number };
  floors: string;            // "지하1층-지상5층"
  approvalDate: string;      // 사용승인일
  acquisitionDate: string;
  acquisitionPrice: number;
  rental: { area: number; rate: number; vacancy: number }; // 0~100 (%)
  tenant: string;
  lat: number; lng: number;
};

type Equipment = {
  id: string; name: string;  // "POS(계산대)" 등 카테고리
  inventory: { hq: number; store: number; logistics: number };
  purchase: { hq: number; store: number; logistics: number };
  transfer: { hq: number; store: number; logistics: number };
  disposal: { hq: number; store: number; logistics: number };
};

type Store = {
  name: string;              // 조직명 / 매장명
  site_type: '본사' | '매장' | '물류';
  asset_value: number;       // 당월말 장부가 합계
  asset_count: number;       // 항목 수
  supply_value: number;      // 비품 재고액
  asset_by_type: Record<string, number>;   // 자산유형 → 금액
  supply_by_category: Record<string, number>;       // 중분류 → 금액
  supply_by_category_count: Record<string, number>; // 중분류 → 수량
};

type MonthlyData = {           // S.monthly[key][monthIdx 0~11]
  '총자산규모': number[];
  '건물자산금액': number[];
  '비품재고액': number[];
  '비품구매금액': number[];
  '비품이동금액': number[];
  '비품폐기금액': number[];
};

type M0Data = {                // 외부 import / xlsb 업로드 시
  meta: { current_period: string; previous_period: string; source: string };
  current: { asset_kpi, location, supplies_kpi, asset_matrix, supplies_category_detail, stores };
  previous: { /* 동일 구조 */ };
  mom: { total, tangible, intangible, supplies, supplies_kpi: {...} };
};
```

### 3.3 영속성
- **LocalStorage 키**: `aspd_bd / aspd_eq / aspd_imgs / aspd_m / aspd_sum`
- **스키마 버전 키**: `aspd_schema_ver` — 불일치 시 전체 wipe + defBd 재구성 (가짜 데모 데이터 잔존 방지)
- **이미지 저장**: 사용자 업로드는 base64로 LocalStorage; xlsx 임베드 사진은 `EMBEDDED_BUILDING_PHOTOS` 상수
- **사업장 데이터**: `EMBEDDED_M0_STORES_DATA` 인라인 (xlsb 추출 2,015개)

---

## 4. 핵심 기능 흐름

### 4.1 첫 로드
1. `init()`:
   - 스키마 버전 검사 → stale 캐시 삭제
   - `__EMBEDDED__.bd ?? defBd()` 로 S.bd 채움 (15건물)
   - LocalStorage 복원(이미지·monthly·summary)
   - `generateM0StoresFromLedger()` → M0_STORES (2,015개)
   - `setRole('exec', ...)` 기본 경영진 뷰
   - sticky 높이 측정 (`--topbar-h`, `--mobile-role-h`)

### 4.2 월마감 xlsb 업로드 → MoM
1. 공유 메뉴 → "월마감 xlsb 업로드 → MoM"
2. `handleMonthlyXlsbUpload(file)`:
   - `XLSX.read(buf)` 로 시트 파싱
   - `자산현황` row 5 → 총자산/유형/무형/비품
   - `비품(프로세스별)` row 5 → 재고/구매/이동/폐기
   - `원장_비품_재고` → 카테고리·사업장 집계
   - `원장_자산` → 사업장별 장부가 집계
   - 현재 화면값 = previous, 업로드 = current 로 M0_DATA 구성
   - `applyM0Data(data)` 호출 → hero/KPI/표/MoM 배지 일괄 갱신

### 4.3 역할 전환
- `setRole(role)`:
  - `ROLE_POLICY[role].mainTabs` 기준으로 탭 가시성 토글
  - store 역할이면 메인 탭 숨기고 사업장 검색 뷰만 표시
  - admin은 추가 패널 표시

### 4.4 건물 상세 드로어
- 카드 클릭 → `openBldDetail(idx)`:
  - 1순위 사진: `S.imgs[idx]` (사용자 업로드)
  - 2순위: `EMBEDDED_BUILDING_DETAILS[name]` (4건 인라인)
  - 3순위: `EMBEDDED_BUILDING_PHOTOS[name]` (정면 사진 재활용)
  - 탭: 기본정보·임대현황·관리이력·메모(LocalStorage)·지도(Kakao)
  - 지도 카테고리 칩: 편의점(CS2)·카페(CE7)·주차장(PK6)·은행(BK9) — 700m 반경 categorySearch

### 4.5 PDF 보고서
- 역할별 섹션 구성 → 모달 미리보기 → `window.print()`
- @media print: 페이지 잘림 방지(`break-inside: avoid`)

---

## 5. UI/UX 원칙 (현재 적용)

- **계층 1개**: 화면당 강조 1순위(hero KPI). Primary CTA 1개(공유)
- **여백 시스템**: 4px/8px 기반, 16/20/24/32px 스케일
- **컬러**: 무채색 90% + 시맨틱(danger·warning·info·success). 다이소 다크네이비 `#1E3A5F`
- **타이포**: 28+ / 13~14 / 10~11 (3단계). Pretendard Variable + tabular-nums
- **모션**: ≤200ms, transition 구체 지정 (transition-all 금지)
- **접근성**: 터치 ≥44px (모바일), 명도대비 4.5:1, focus-visible outline

### 모바일 우선 반응형 (768/430/360 breakpoints)
- 가로 스크롤 0 (`overflow-x:hidden`, `max-width:100vw`)
- 모든 grid → `1fr`, hero 숫자 `clamp(26~48px)`
- 모달 `100dvh` (iOS 100vh 회피)
- 칩/탭 가로 스크롤, input `font-size:16px`(iOS 줌 차단)

---

## 6. 컴포넌트 카탈로그

| 컴포넌트 | 사용처 | 역할 |
|---|---|---|
| `MetricCard` (`.ac-card`) | KPI | 숫자 + 라벨 + 비교 |
| `Donut` (Chart.js) | hero·자산구성·매장 | 카테고리 비중 |
| `MomBadge` (`.ac-pill`) | KPI 옆 | ±%, 색상 자동 |
| `BuildingCard` (`.bld`) | 건물 그리드 | 사진 + 주소 + 임대율 |
| `BuildingDrawer` | 건물 상세 | 5탭 풀화면 패널 |
| `RoleStrip` | topbar | 역할 토글 4종 |
| `ChipFilter` | 건물 상세 | 지역/용도 필터 칩 |
| `Toast` | 전역 | 행동 결과 알림 |
| `Modal` | 공유/PDF/관리자 | 풀스크린(모바일) |
| `EmptyState` (`.bld-img-ph`) | 사진 미등록 | 아이콘+상태+CTA (UIUX #8.5) |

---

## 7. 사진 시스템

- **마스터**: `EMBEDDED_BUILDING_PHOTOS` (15건 정면, base64 inline)
- **상세 팝업**: `EMBEDDED_BUILDING_DETAILS` (4건 디테일 컷)
- **사용자 업로드**: `사진 등록` 버튼 → 800×600/150KB로 압축 → LocalStorage
- **Fallback 체인**: 사용자 업로드 → 이름 매칭 임베드 → 인덱스 매칭 임베드 → 외부 매칭 → 빈 상태 카드
- **공유 호환성**: 기본 "단일 HTML(이미지 포함)" → 받는 사람은 파일 1개만 열면 됨

---

## 8. 권한·관리자 모드

- `setRole('admin', btn)` → 비밀번호 모달
- 인증 후: `데이터 조정` 탭 + 관리자 패널 노출
- 업로드 핸들러 5종:
  - `asset` (자산 매트릭스)
  - `ledger_asset` (원장 자산)
  - `ledger_eq` (원장 비품 재고)
  - `eq_ops` (비품 운영)
  - `buildings` (건물현황)

---

## 9. 기술 스택

| 영역 | 선택 |
|---|---|
| 패키징 | **단일 HTML** (모든 자산 base64 inline) |
| 차트 | Chart.js 4.4 (CDN) |
| 엑셀 | SheetJS xlsx 0.18 (CDN) — xlsx/xlsb/csv 모두 지원 |
| 폰트 | Pretendard Variable (CDN) |
| 지도 | Kakao Maps SDK + services (Places API) |
| 상태 | 단일 모듈 전역 변수 `S` + LocalStorage 영속 |
| 배포 | GitHub Pages (`gh-pages` 또는 `main` 루트) |

---

## 10. 재개발 시 권장 아키텍처 (선택)

> 현재 단일 HTML 구조를 유지하거나, 아래 중 선택해 마이그레이션 가능.

| 옵션 | 적합 상황 |
|---|---|
| **단일 HTML 유지(현재)** | 인프라 0 의존, 이메일 공유, 빠른 배포 |
| **Next.js + RSC** | 사용자 다수, 인증, 권한, 실시간 데이터, SEO |
| **Vite + React + Zustand** | SPA, 컴포넌트 재사용성, 향후 모바일 앱 |
| **SvelteKit** | 가벼운 인터랙티브, 적은 번들 크기 |

### 데이터 백엔드 옵션
- **Supabase**: 인증 + Postgres + Row-Level Security + 실시간 (xlsb는 별도 ETL)
- **NocoDB / Airtable**: 비개발자 입력 가능, API 자동 생성
- **자체 ETL + S3/Blob**: xlsb를 nightly로 파싱해 JSON 캐시

### 추천 폴더 구조 (Next.js 기준)
```
app/
  (dashboard)/
    page.tsx          # 자산현황(개요)
    detail/page.tsx   # 자산 상세
    drilldown/page.tsx
    buildings/page.tsx
    buildings/[id]/page.tsx  # 건물 드로어 (parallel route)
    stores/page.tsx
    admin/page.tsx
  api/
    upload/[type]/route.ts
    mom/route.ts
components/
  cards/  charts/  filters/  modals/  drawer/
lib/
  parsers/xlsb.ts  parsers/xlsx.ts
  data/buildings.ts  stores.ts  monthly.ts
  state/store.ts (Zustand)
public/
  buildings/*.jpg
  details/*.jpg
  og-cover.png
```

---

## 11. 마이그레이션 체크리스트

### 데이터
- [ ] 15건물 마스터 → `lib/data/buildings.ts`
- [ ] 좌표 15쌍 → `BUILDING_COORDS`
- [ ] 사진 19장(정면 15 + 디테일 4) → `public/buildings/`
- [ ] xlsb 파서 → API 라우트 또는 ETL
- [ ] 사업장 2,015개 + 카테고리 41개 → DB 시드

### 기능
- [ ] MoM 자동 계산 로직
- [ ] 사진 fallback 체인 (4단계)
- [ ] 역할별 가시성 정책
- [ ] LocalStorage → 서버 사이드 영속 (사용자 업로드 사진, 메모)
- [ ] PDF 보고서 (puppeteer 또는 react-pdf)
- [ ] Kakao Maps 카테고리 검색

### 비기능
- [ ] OG/favicon 메타
- [ ] 모바일 반응형 (≤768/≤430/≤360)
- [ ] 접근성 (focus-visible, ARIA, 터치 ≥44px)
- [ ] 다크모드 (선택)
- [ ] 다국어 (선택)
- [ ] 분석/관측 (Plausible, Sentry)

---

## 12. 알려진 한계 / 개선 우선순위

| 우선 | 항목 | 비고 |
|---|---|---|
| ⭐ | 사용자 업로드 사진은 LocalStorage 전용 | 다른 PC와 공유 안 됨 → 서버 백엔드 필요 |
| ⭐ | xlsb 업로드는 클라이언트 파싱 (대용량 시 느림) | 서버 ETL로 이전 권장 |
| 2 | 차트 라벨이 모바일에서 잘림 | Chart.js 옵션 모바일 분기 |
| 3 | 다국어 미지원 | 한글 전용 |
| 4 | 단일 HTML 3.4MB | 코드 분리 + lazy load 시 800KB 가능 |
| 5 | 사진 갱신은 코드 재배포 필요 | CMS 백엔드 도입 시 해결 |

---

## 13. 라이브 / 저장소

- **운영 URL**: https://gerrardbak08.github.io/asset-dashboard/
- **저장소**: https://github.com/gerrardbak08/asset-dashboard
- **소유 파일**: `C:\Users\sjowo\OneDrive\바탕 화면\SJ_대시보드_V16.html`

---

## 14. 의사결정 로그 (현 시점까지의 핵심 결정)

| 결정 | 이유 |
|---|---|
| 단일 HTML로 시작 | 인프라 0, 이메일 공유, 빠른 검증 |
| 사진 base64 인라인 | 받는 쪽이 폴더 없이 파일 1개로 작동 |
| 사업장 2,015건 인라인 | 검색 누락 0 보장 (서버 의존 회피) |
| Kakao Maps | 한국 매장 좌표 + 로드뷰 + 카테고리 검색 풍부 |
| 역할 4종 | 임원/팀/매장/관리자 권한 분리 |
| MoM은 항상 화면값 vs 업로드값 | "현재 보이는 값"의 의미 일관 |
| 가짜 데모 데이터 강제 폐기 | 인천 송도 타워 등 잘못된 정보 노출 차단 |

---

## 15. 변경 이력 (요약)

- v15.x: 초기 단일 HTML, 사진 0장
- V16.0: 사진 6장(xlsx 임베드) + 빈 상태 UX
- V16.1: 부산허브·남사 사진 (Naver) + 모든 15건물 사진 + 디테일 4건
- V16.2: 사업장 2,015개 인라인 + xlsb MoM 업로드
- V16.3: 모바일 우선 반응형 + GitHub Pages 배포 + OG/favicon

---

> 이 문서는 "현재 상태 그대로"의 기획안입니다. 새 시스템으로 옮길 때는 11장 마이그레이션 체크리스트를 따라 진행하세요.
