# Project Retrospective: Deployment & UI/UX Optimization Sprint

## 1. 개요 (Overview)
본 문서는 자산관리 플랫폼의 Railway 운영 배포 과정에서 발생한 시행착오와 해결 과정을 기록하고, 향후 동일한 실수를 반복하지 않기 위한 복기 내용을 담고 있습니다.

## 2. 주요 실패 사례 및 해결 (Failures & Solutions)

### A. 정적 자산(건물 사진) 404 오류
- **현상**: DB에는 사진 경로가 존재하나, 웹 화면에서 사진이 로딩되지 않음.
- **실패 원인**: 
  - Railway(Nixpacks) 배포 환경의 특성을 간과함. 빌드 스테이지와 실행 스테이지가 분리되어 있어, 루트 디렉토리의 `uploads/` 폴더가 실행 환경으로 복사되지 않음.
  - 상대 경로(`../../uploads`) 접근 방식이 컨테이너 환경에서 유효하지 않음.
- **해결책**:
  - `railway.toml`의 `buildCommand` 단계에서 `uploads/` 내 이미지를 API 서비스의 `public/files/` 디렉토리로 직접 복사하도록 수정.
  - 서버 설정(`server.ts`)에서 `public` 폴더를 루트 정적 경로로 지정하여 일관된 접근성 확보.
- **교훈**: 컨테이너 환경에서는 모든 실행 자산이 서비스 패키지 내부에 포함되거나 볼륨으로 마운트되어야 함을 명심할 것.

### B. UI/UX: 활동 타임라인 및 테이블 레이아웃
- **현상**: 모바일에서 텍스트가 두 줄로 겹치거나, 타임라인 카드의 높이가 부족하여 내용이 가려짐.
- **실패 원인**:
  - `flex-1` 등에만 의존하고 최소 높이(`min-h`)를 명시하지 않아 컨테이너가 붕괴됨.
  - 표 데이터에서 `whitespace-nowrap`을 생략하여 좁은 화면에서 가독성이 저하됨.
  - `StoreTimeline`이 포함된 Flex 컨테이너에 `min-w-0`이 없어 가로로 레이아웃이 확장(oversize)되는 현상 발생.
  - 타임라인 내부 텍스트 크기가 커서 카드 너비를 불필요하게 차지하고, 이로 인해 대시보드 전체 레이아웃이 흔들리는 현상 확인.
  - 노트북 등 낮은 해상도의 화면에서 상단 카드들의 높이 때문에 하단 타임라인 카드가 잘려 보이는 현상 지속.
  - 복잡한 한 줄짜리 배포 스크립트가 Railway 환경에서 불규칙한 빌드 실패 유발.
- **해결책**:
  - 타임라인 텍스트 크기를 `text-micro` 및 `text-[10px]`로 축소하고 항목 너비를 `w-[160px]`로 최적화.
  - 상단 차트 그리드 높이를 `h-[180px]`로 대폭 압축하고 KPI 카드 패딩을 줄여 전체 콘텐츠를 한 화면(Single View)에 최적화.
  - 우측 패널에 `overflow-y-auto`를 적용하고 하단 여백(`pb-12`)을 충분히 확보하여 절대 짤림이 없도록 구조적 개선.
  - 빌드 프로세스를 `scripts/build.sh`로 모듈화하여 배포 안정성 및 가독성 확보.
  - 타임라인 메인 선 위에 고정 점(Dot)을 배치하고 아이콘까지 점선(Dashed line)으로 연결하여 프리미엄 비주얼 구현.
  - 모든 주요 테이블(임대, 관리, 사용자 목록)에 `whitespace-nowrap` 및 `overflow-x-auto`를 적용하여 모바일 대응력 강화.
- **교훈**: 데이터 중심 UI는 줄바꿈보다 가로 스크롤이 가독성 면에서 유리하므로 이를 기본 원칙으로 삼을 것.

### C. 다크모드 스타일 회귀
- **현상**: 다크모드 최적화 과정에서 라이트모드의 CI 영역 배경색까지 네이비로 바뀌어 디자인 일관성 훼손.
- **실패 원인**: "사이드바 배경과 맞춤"이라는 요구사항을 테마 구분 없이 전역 스타일로 적용함.
- **해결책**: Tailwind의 `dark:` 접두사를 사용하여 모드별 색상을 명확히 분리 (`bg-white dark:bg-sidebar`).
- **교훈**: 테마 관련 수정 시 항상 양쪽 모드에서의 시각적 변화를 독립적으로 검증할 것.

## 3. 코드 품질 및 배포 원칙 (Principles)
1. **사전 검증**: 푸시 전 반드시 `pnpm -r exec tsc --noEmit`을 통해 타입 체크를 수행하여 빌드 실패를 방지한다.
2. **맥락 이해**: 기능 추가 시 해당 기능이 사용되는 기기 환경(모바일/웹)과 사용자의 의도(데이터 가시성/브랜드 아이덴티티)를 우선적으로 고려한다.
3. **기록의 생활화**: 해결된 이슈는 반드시 문서화하여 프로젝트의 자산으로 남긴다.

## 4. 향후 과제 (Next Steps)
- 비품 현황 및 건물 정보 조정 기능의 실구현 (Next Release).
- 대용량 이미지 업로드 시 Sharp 라이브러리를 통한 자동 리사이징 및 WebP 변환 고도화.
- 다크모드 전용 컬러 팔레트의 세부 미세 조정.

---
*Last Updated: 2026-05-12*
